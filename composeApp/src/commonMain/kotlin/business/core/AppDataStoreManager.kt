package business.core

//import common.Context

const val APP_DATASTORE = "com.razzaghi.shoppingbykmp" // TODO: What is it?!
/*
class AppDataStoreManager(val context: Context) : AppDataStore { // TODO: implement Context

    override suspend fun setValue(
        key: String,
        value: String
    ) {
        context.putData(key, value)
    }

    override suspend fun readValue(
        key: String,
    ): String? {
        return context.getData(key)
    }
}

 */
